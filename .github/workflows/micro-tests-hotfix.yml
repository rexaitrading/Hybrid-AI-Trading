name: Micro Tests Hotfix (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled micro-tests-hotfix placeholder
        run: echo "micro-tests-hotfix.yml temporarily disabled (CI guardrail = python-ci only)"