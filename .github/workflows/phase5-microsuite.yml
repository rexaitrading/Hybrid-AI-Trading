name: Legacy – Disabled
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled legacy workflow
        run: echo "This workflow is disabled and kept only for history."