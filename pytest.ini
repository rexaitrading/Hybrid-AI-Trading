# pytest.ini
[pytest]
# Only discover tests inside the tests/ folder
testpaths = tests

# Only treat files starting with test_*.py as tests
python_files = test_*.py

# Optional: custom markers
markers =
    perf: performance tests
    integration: integration tests
    unit: unit tests
