[pytest]
# Only look in the smoke suite by default
testpaths = tests/smoke
python_files = test_*.py

# Markers (documented so -m works)
markers =
    smoke: lightweight checks
    unit: unit tests
    integration: external/API dependent tests
    perf: performance tests

# Be quiet on test collection warnings
addopts = -ra
