[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs =
    .git
    .venv
    venv
    env
    build
    dist
    scratch
    docs
    logs
    src/.intel
addopts =
    -ra
    --strict-markers