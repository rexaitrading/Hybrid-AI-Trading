[pytest]
# Keep test output concise in CI, but still fail tests properly
addopts = -q

# Silence specific noisy deprecation warnings from eventkit only.
# Format: action:message-regex:category:module-regex
filterwarnings =
    ignore:There is no current event loop:DeprecationWarning:eventkit\.util
    ignore::DeprecationWarning:eventkit\..*
